{
  "name": "ccdata-yf-05-license-lens",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview",
    "test:logic": "mkdir -p node_modules/.tmp && rolldown selftest/selftest.ts --platform node --format cjs --file node_modules/.tmp/selftest.cjs && node node_modules/.tmp/selftest.cjs",
    "test:ui": "mkdir -p node_modules/.tmp && rolldown selftest/ui-smoke.tsx --platform node --format cjs --file node_modules/.tmp/ui-smoke.cjs && node node_modules/.tmp/ui-smoke.cjs",
    "test": "npm run test:logic && npm run test:ui"
  },
  "dependencies": {
    "@vitejs/plugin-react": "latest",
    "vite": "latest",
    "typescript": "latest",
    "react": "latest",
    "react-dom": "latest",
    "lucide-react": "latest"
  },
  "devDependencies": {
    "@types/react": "latest",
    "@types/react-dom": "latest"
  }
}
